import Component from "@core/Component";

class Profile extends Component {
    render() {
        return `
            <main>
                {{{ Profile }}}
            </main>
        `;
    }
}

export default Profile;
